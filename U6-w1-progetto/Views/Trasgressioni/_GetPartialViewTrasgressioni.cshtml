@using U6_w1_progetto.Models
@model List<Trasgressione>
@foreach (Trasgressione item in Model)
{
    <div class="d-flex container border my-2">
        <p class="me-3"> @item.id</p>
        <p> @item.descrizione  </p>

    </div>
}